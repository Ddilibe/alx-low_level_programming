#include "main.h"

/**
 * main - Check the code
 *
 * Return: Always 0
 */

int main(void)
{
	char *str;

	str = "I do not fear computers. i fear thr lack of them - Isaac Asimov";
	print_rev(str);
	return (0);
}
