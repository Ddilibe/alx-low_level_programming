#include "main.h"
#include <string.h>
#include <stdio.h>
/**
 * rev_string - function to reverse a string
 * @s: the string input
 * Return: No return
 */

void rev_string(char *s)
{
	int count = 0, i, j;
	char *str, temy;	
}
